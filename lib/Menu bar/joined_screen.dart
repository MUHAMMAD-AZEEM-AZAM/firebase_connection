import 'package:flutter/material.dart';

class JoinedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Joined Screen'),
      ),
      body: Center(
        child: Text('This is the Joined Screen'),
      ),
    );
  }
}
